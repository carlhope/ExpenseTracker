<Window x:Class="ExpenseTracker.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExpenseTracker"
        mc:Ignorable="d"
        Title="Expense History" Height="400" Width="350"
        WindowStartupLocation="CenterScreen">


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="1" Content="View Expenses History" Margin="10,0,10,10" HorizontalContentAlignment="Center" HorizontalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Margin="10,0,10,20" Height="40" FontSize="16"
          ItemsSource="{Binding Categories}"
          SelectedItem="{Binding SelectedCategory}"/>

        <ScrollViewer VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly" Grid.Row="2">
            <ListBox ItemsSource="{Binding FilteredExpenses}" Margin="0,10,0,0" FontSize="16" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,0,1" BorderBrush="LightGray" Padding="5">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Description}" FontWeight="Bold"/>
                                <TextBlock>
                                <Run Text="Category: "/>
                                <Run Text="{Binding Category}" />
                                </TextBlock>
                                <TextBlock>
                                <Run Text="Amount: £"/>
                                <Run Text="{Binding Amount}" />
                                </TextBlock>
                                <TextBlock Text="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>

</Window>
